VITE_API_URL=http://localhost:4000

# PostHog product analytics. The project key is public (it ships in the client
# bundle); set your own here. Empty => analytics is disabled.
VITE_POSTHOG_KEY=
# Optional: PostHog API host (for self-hosted or EU cloud)
# VITE_POSTHOG_HOST=
